image: python:3
variables:
  GIT_SUBMODULE_STRATEGY: recursive
pages:
  stage: deploy
  script:
    - mkdir public
    - mv index.html public/html
    - mv revealjs public/revealjs
  artifacts:
    paths:
    - public
  only:
  - master
